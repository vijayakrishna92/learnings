# This script demonstrates how to take input from the user in Python.
a = input('Enter a number: ') # input() function is used to take input from the user
# input() function returns a string, so we need to convert it to int or float if needed
b = input('Enter name: ') 